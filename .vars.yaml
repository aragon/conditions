# Manifest of env vars resolved by `vars` (https://github.com/vars-cli/vars).
# Optional — if you don't use `vars`, populate `.env` from `.env.example` instead.

keys:
  - DEPLOYER_KEY
  - ETHERSCAN_API_KEY
  - REFUND_ADDRESS
  # Project-specific (optional)
  - DAO_ADDRESS
  - SAFE_ADDRESS
